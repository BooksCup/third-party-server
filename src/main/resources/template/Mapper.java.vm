package ${package}.mapper;

import ${package}.entity.${className};

/**
 * ${comments}
 *
 * @author ${author}
 * @date ${datetime}
 */
public interface ${className}Mapper {

    /**
     * 新增${comments}
     *
     * @param ${classname} ${comments}
     */
    void add${className}(${className} ${classname});

}